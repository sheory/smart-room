name: FastAPI CI/CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  docker:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Create .env file
      run: |
        echo "POSTGRES_DB=your_db_name" > .env
        echo "POSTGRES_USER=your_db_user" >> .env
        echo "POSTGRES_PASSWORD=your_db_password" >> .env
        echo "DB_HOST=your_db_host" >> .env
        echo "DB_PORT=5432" >> .env

    - name: Start containers
      run: docker compose -f "docker-compose.yml" up -d --build

    - name: get envs
      run: docker exec -it fastapi_app env | grep POSTGRES

    - name: Run migrations
      run: |
        docker exec fastapi_app alembic upgrade head

    - name: Run tests with coverage
      run: |
        docker exec fastapi_app pytest --cov=app --cov-fail-under=95 tests/

    - name: Stop containers
      if: always()
      run: docker compose -f "docker-compose.yml" down